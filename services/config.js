/*
* This file is used as an alternate to do programatic configurations like
* the server port and is supposed to be a handy substitue for environment variables
*/

module.exports = {
  SERVER_PORT: 3030 // The port on which express server will listen on
}
